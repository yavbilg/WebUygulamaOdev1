@{
    ViewData["Title"] = "Diagnostic Test";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Static Files Test</title>
    
    <!-- Test 1: Bootstrap from wwwroot -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    
    <!-- Test 2: Font Awesome from CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    
    <!-- Test 3: Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" />
    
    <style>
        .test-section {
            padding: 20px;
            margin: 20px 0;
            border: 2px solid #ccc;
            border-radius: 10px;
        }
        .success { background-color: #d4edda; border-color: #c3e6cb; }
        .error { background-color: #f8d7da; border-color: #f5c6cb; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-5">🔍 Static Files Diagnostic Test</h1>
        
        <!-- Test 1: Bootstrap -->
        <div class="test-section">
            <h2>Test 1: Bootstrap CSS</h2>
            <p>Path: <code>~/lib/bootstrap/dist/css/bootstrap.min.css</code></p>
            <div class="alert alert-info">
                <strong>Test:</strong> Bu kutu mavi renkte ve güzel şekilde stillendirilmiş olmalı
            </div>
            <button class="btn btn-primary">Primary</button>
            <button class="btn btn-success">Success</button>
            <button class="btn btn-danger">Danger</button>
            <button class="btn btn-warning">Warning</button>
            <div class="mt-3">
                <span class="badge bg-primary">Badge</span>
                <span class="badge bg-success">Badge</span>
            </div>
        </div>
        
        <!-- Test 2: Font Awesome -->
        <div class="test-section">
            <h2>Test 2: Font Awesome (CDN)</h2>
            <p>Path: <code>https://cdnjs.cloudflare.com/.../font-awesome.min.css</code></p>
            <div style="font-size: 2rem;">
                <i class="fas fa-check-circle text-success"></i> Check
                <i class="fas fa-times-circle text-danger"></i> Times
                <i class="fas fa-home text-primary"></i> Home
                <i class="fas fa-user text-info"></i> User
                <i class="fas fa-heart text-danger"></i> Heart
            </div>
            <p class="mt-2">Yukarıda ikonlar görünüyorsa Font Awesome çalışıyor ✓</p>
        </div>
        
        <!-- Test 3: Custom CSS (site.css) -->
        <div class="test-section">
            <h2>Test 3: Custom CSS (site.css)</h2>
            <p>Path: <code>~/css/site.css</code></p>
            <div class="card shadow-lg">
                <div class="card-header bg-gradient-primary text-white">
                    <h5 class="mb-0">Test Card Header</h5>
                </div>
                <div class="card-body">
                    <p>Bu kart gradient arka plana ve gölgeye sahip olmalı</p>
                    <button class="btn btn-primary">Custom Styled Button</button>
                </div>
            </div>
        </div>
        
        <!-- Test 4: JavaScript -->
        <div class="test-section">
            <h2>Test 4: JavaScript Libraries</h2>
            <p>jQuery: <code>~/lib/jquery/dist/jquery.min.js</code></p>
            <p>Bootstrap JS: <code>~/lib/bootstrap/dist/js/bootstrap.bundle.min.js</code></p>
            <div id="js-test-result" class="alert alert-secondary">
                JavaScript test yükleniyor...
            </div>
            <button class="btn btn-info" onclick="testJS()">Test JavaScript</button>
        </div>
        
        <!-- Test 5: Network Requests -->
        <div class="test-section">
            <h2>Test 5: Network Inspection</h2>
            <p>F12'ye basın ve Network sekmesine gidin</p>
            <ol>
                <li>Sayfayı yenileyin (F5)</li>
                <li>Aşağıdaki dosyaları arayın:
                    <ul>
                        <li>bootstrap.min.css (Status: 200 ✓)</li>
                        <li>site.css (Status: 200 ✓)</li>
                        <li>jquery.min.js (Status: 200 ✓)</li>
                        <li>bootstrap.bundle.min.js (Status: 200 ✓)</li>
                    </ul>
                </li>
                <li>Eğer 404 görüyorsanız, dosya yüklenmemiş demektir ✗</li>
            </ol>
        </div>
        
        <!-- Test Results Summary -->
        <div class="test-section" style="background: #e7f3ff;">
            <h2>📊 Test Sonuçları</h2>
            <div id="test-results">
                <p>Konsolu açın (F12) ve sonuçları görün</p>
            </div>
        </div>
    </div>
    
    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Test JavaScript
        function testJS() {
            var result = document.getElementById('js-test-result');
            result.innerHTML = '<strong>✓ JavaScript Çalışıyor!</strong>';
            result.className = 'alert alert-success';
        }
        
        // Auto test on page load
        window.onload = function() {
            var results = [];
            
            // Test jQuery
            if (typeof jQuery !== 'undefined') {
                results.push('✓ jQuery yüklendi (v' + jQuery.fn.jquery + ')');
                console.log('✓ jQuery loaded:', jQuery.fn.jquery);
            } else {
                results.push('✗ jQuery yüklenmedi');
                console.error('✗ jQuery not loaded');
            }
            
            // Test Bootstrap
            if (typeof bootstrap !== 'undefined') {
                results.push('✓ Bootstrap JS yüklendi');
                console.log('✓ Bootstrap loaded');
            } else {
                results.push('✗ Bootstrap JS yüklenmedi');
                console.error('✗ Bootstrap not loaded');
            }
            
            // Test CSS by checking computed styles
            var testElement = document.querySelector('.btn-primary');
            if (testElement) {
                var bgColor = window.getComputedStyle(testElement).backgroundColor;
                if (bgColor && bgColor !== 'rgba(0, 0, 0, 0)' && bgColor !== 'transparent') {
                    results.push('✓ Bootstrap CSS yüklendi (button color: ' + bgColor + ')');
                    console.log('✓ Bootstrap CSS loaded');
                } else {
                    results.push('✗ Bootstrap CSS yüklenmedi');
                    console.error('✗ Bootstrap CSS not loaded');
                }
            }
            
            // Display results
            var resultsDiv = document.getElementById('test-results');
            resultsDiv.innerHTML = '<ul>' + results.map(r => '<li>' + r + '</li>').join('') + '</ul>';
            
            // Console summary
            console.log('=== STATIC FILES TEST SUMMARY ===');
            results.forEach(r => console.log(r));
            console.log('Current URL:', window.location.href);
            console.log('Protocol:', window.location.protocol);
            console.log('Host:', window.location.host);
        };
    </script>
</body>
</html>
